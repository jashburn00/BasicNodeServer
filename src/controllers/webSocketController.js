const SocketStatuses = require('../utils/SocketStatuses.js');


const handleMessage = (message, socket) => { 
	switch (SocketStatuses){

	}
};

const handleClose = (socketId)



module.exports = {
	handleMessage,
	handleInput,
	handleChatRequest,
	handleDrawingRequest,
	handleClose
};
